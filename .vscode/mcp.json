{
	"mcpServers": {
		"postgres": {
			"command": "docker",
			"args": [
				"run",
				"-i",
				"--rm",
				"mcp/postgres",
				"postgresql://postgres:password@host.docker.internal:5432/mydatabase"
			]
		}
	}
}